[package]
name = "dsx-git"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "DSX git integration: status, diff, checkpoint, commit"

[dependencies]
anyhow.workspace = true
